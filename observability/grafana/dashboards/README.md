# Grafana Dashboards

Dashboards will be added in the observability phase.

For the MVP, use Prometheus directly to inspect:

- `llm_backend_health`
- `llm_loaded_models`
- `llm_healthcheck_latency_seconds`
- `llm_healthcheck_errors_total`
